--- before
+++ after
@@ -844,11 +844,11 @@
     if (note == (*((tones + ofs) + 7)))
     {
       chan = getNextFreeChannel(chan);
-      if (chan == (-1))
+      if ((mask = -1) == chan)
       {
         break;
       }
-      SsUtKeyOnV(chan, vabId, prog, (0, i ^ 0), note, (i < 0x10) * 0, 0x7F, 0x7F);
+      SsUtKeyOnV(chan, vabId, prog, (0, 0 ^ i), note, (i < 0x10) * 0, 0x7F, 0x7F);
       mask |= 1 << chan;
     }
   }
