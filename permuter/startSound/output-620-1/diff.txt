--- before
+++ after
@@ -833,13 +833,15 @@
   int32_t mask;
   char *tones;
   unsigned int chan;
-  int16_t i;
+  int i;
   int32_t ofs;
+  int32_t new_var;
   mask = 0;
   tones = ((char *) VHB_HEADER_ADDR[vabId]) + 0x820;
   tones = inline_fn(tones, prog);
   chan = 0x18;
-  for (i = 0, ofs = 0; i < 0x10; i++, ofs += 0x20)
+  new_var = (ofs = 0);
+  for (i = 0, new_var; i < 0x10; i++, ofs += 0x20)
   {
     if (note == (*((tones + ofs) + 7)))
     {
