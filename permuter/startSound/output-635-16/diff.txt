--- before
+++ after
@@ -830,6 +830,7 @@
 
 uint32_t startSound(int32_t vabId, char prog, char note)
 {
+  char new_var;
   int32_t mask;
   char *tones;
   unsigned int chan;
@@ -838,22 +839,15 @@
   mask = 0;
   tones = ((char *) VHB_HEADER_ADDR[vabId]) + 0x820;
   tones = inline_fn(tones, prog);
+  new_var = note;
   chan = 0x18;
   for (i = 0, ofs = 0; i < 0x10; i++, ofs += 0x20)
   {
     if (note == (*((tones + ofs) + 7)))
     {
       chan = getNextFreeChannel(chan);
-      if (chan == (-1))
-      {
-        break;
-      }
-      SsUtKeyOnV(chan, vabId, prog, (0, i ^ 0), note, (i < 0x10) * 0, 0x7F, 0x7F);
-      mask |= 1 << chan;
-    }
-  }
-
-  return mask;
+      mask = 1;
+ if (chan == (-mask)) { break; } SsUtKeyOnV(chan, vabId, prog, (0, i ^ 0), new_var, (i < 0x10) * 0, 0x7F, 0x7F); mask |= 1 << chan; } } return mask;
 }
 
 void _stopSoundMask(uint32_t mask);
