--- before
+++ after
@@ -833,6 +833,7 @@
   int32_t mask;
   char *tones;
   unsigned int chan;
+  int32_t new_var;
   int16_t i;
   int32_t ofs;
   mask = 0;
@@ -844,11 +845,13 @@
     if (note == (*((tones + ofs) + 7)))
     {
       chan = getNextFreeChannel(chan);
+      new_var = vabId;
       if (chan == (-1))
       {
         break;
       }
-      SsUtKeyOnV(chan, vabId, prog, (0, i ^ 0), note, (i < 0x10) * 0, 0x7F, 0x7F);
+      vabId = vabId;
+      SsUtKeyOnV(chan, new_var, prog, (0, i ^ 0), note, (i < 0x10) * 0, 0x7F, 0x7F);
       mask |= 1 << chan;
     }
   }
