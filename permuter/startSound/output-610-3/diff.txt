--- before
+++ after
@@ -830,25 +830,29 @@
 
 uint32_t startSound(int32_t vabId, char prog, char note)
 {
+  int new_var2;
   int32_t mask;
   char *tones;
   unsigned int chan;
+  int32_t new_var;
   int16_t i;
   int32_t ofs;
   mask = 0;
   tones = ((char *) VHB_HEADER_ADDR[vabId]) + 0x820;
   tones = inline_fn(tones, prog);
   chan = 0x18;
-  for (i = 0, ofs = 0; i < 0x10; i++, ofs += 0x20)
+  new_var = (ofs = 0);
+  for (i = 0, new_var; i < 0x10; i++, ofs += 0x20)
   {
     if (note == (*((tones + ofs) + 7)))
     {
       chan = getNextFreeChannel(chan);
+      new_var2 = i ^ 0;
       if (chan == (-1))
       {
         break;
       }
-      SsUtKeyOnV(chan, vabId, prog, (0, i ^ 0), note, (i < 0x10) * 0, 0x7F, 0x7F);
+      SsUtKeyOnV(chan, vabId, prog, (0, new_var2), note, (i < 0x10) * 0, 0x7F, 0x7F);
       mask |= 1 << chan;
     }
   }
