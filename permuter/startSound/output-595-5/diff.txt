--- before
+++ after
@@ -833,10 +833,10 @@
   int32_t mask;
   char *tones;
   unsigned int chan;
-  int16_t i;
+  char i;
   int32_t ofs;
+  tones = ((char *) VHB_HEADER_ADDR[vabId]) + 0x820;
   mask = 0;
-  tones = ((char *) VHB_HEADER_ADDR[vabId]) + 0x820;
   tones = inline_fn(tones, prog);
   chan = 0x18;
   for (i = 0, ofs = 0; i < 0x10; i++, ofs += 0x20)
@@ -844,11 +844,12 @@
     if (note == (*((tones + ofs) + 7)))
     {
       chan = getNextFreeChannel(chan);
+      mask = (i < 0x10) * 0;
       if (chan == (-1))
       {
         break;
       }
-      SsUtKeyOnV(chan, vabId, prog, (0, i ^ 0), note, (i < 0x10) * 0, 0x7F, 0x7F);
+      SsUtKeyOnV(chan, vabId, prog, (0, i ^ 0), note, mask, 0x7F, 0x7F);
       mask |= 1 << chan;
     }
   }
