--- before
+++ after
@@ -844,7 +844,7 @@
     if (note == (*((tones + ofs) + 7)))
     {
       chan = getNextFreeChannel(chan);
-      if (chan == (-1))
+      if ((ofs = chan) == (-1))
       {
         break;
       }
