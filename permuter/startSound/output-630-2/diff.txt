--- before
+++ after
@@ -844,11 +844,11 @@
     if (note == (*((tones + ofs) + 7)))
     {
       chan = getNextFreeChannel(chan);
-      if (chan == (-1))
+      if ((mask = chan) == (-1))
       {
         break;
       }
-      SsUtKeyOnV(chan, vabId, prog, (0, i ^ 0), note, (i < 0x10) * 0, 0x7F, 0x7F);
+      SsUtKeyOnV(chan, vabId, prog, (0, i ^ 0), note, ((i - 1) < (0x10 - 1)) * 0, 0x7F, 0x7F);
       mask |= 1 << chan;
     }
   }
