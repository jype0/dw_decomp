--- before
+++ after
@@ -831,6 +831,7 @@
 uint32_t startSound(int32_t vabId, char prog, char note)
 {
   int32_t mask;
+  int new_var;
   char *tones;
   unsigned int chan;
   int16_t i;
@@ -843,12 +844,14 @@
   {
     if (note == (*((tones + ofs) + 7)))
     {
+      new_var = 1;
       chan = getNextFreeChannel(chan);
-      if (chan == (-1))
+      ofs = 0x10;
+      if (chan == (-new_var))
       {
         break;
       }
-      SsUtKeyOnV(chan, vabId, prog, (0, i ^ 0), note, (i < 0x10) * 0, 0x7F, 0x7F);
+      SsUtKeyOnV(chan, vabId, prog, (0, i ^ 0), note, (i < ofs) * 0, 0x7F, 0x7F);
       mask |= 1 << chan;
     }
   }
