--- before
+++ after
@@ -836,12 +836,13 @@
   int16_t i;
   int32_t ofs;
   mask = 0;
-  tones = ((char *) VHB_HEADER_ADDR[vabId]) + 0x820;
+  tones = (char *) VHB_HEADER_ADDR[vabId];
+  tones = tones + 0x820;
   tones = inline_fn(tones, prog);
   chan = 0x18;
   for (i = 0, ofs = 0; i < 0x10; i++, ofs += 0x20)
   {
-    if (note == (*((tones + ofs) + 7)))
+    if (note == ((((((((((((*((tones + ofs) + 7)) & 0xFF) & 0xFF) & 0xFF) & 0xFF) & 0xFF) & 0xFF) & 0xFF) & 0xFF) & 0xFF) & 0xFF) & 0xFF))
     {
       chan = getNextFreeChannel(chan);
       if (chan == (-1))
