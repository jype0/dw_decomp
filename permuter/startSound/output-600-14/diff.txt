--- before
+++ after
@@ -844,11 +844,12 @@
     if (note == (*((tones + ofs) + 7)))
     {
       chan = getNextFreeChannel(chan);
+      mask = chan;
       if (chan == (-1))
       {
         break;
       }
-      SsUtKeyOnV(chan, vabId, prog, (0, i ^ 0), note, (i < 0x10) * 0, 0x7F, 0x7F);
+      SsUtKeyOnV(mask, vabId, prog, (0, i ^ 0), note, (i < 0x10) * 0, 0x7F, 0x7F);
       mask |= 1 << chan;
     }
   }
