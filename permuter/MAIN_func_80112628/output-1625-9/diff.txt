--- before
+++ after
@@ -2852,7 +2852,7 @@
   struct DIRENTRY *next;
   char system[8];
 };
-long MemCardGetDirentry(long chan, char *name, struct DIRENTRY *pdir, long *files, long ofs, long max);
+long MemCardGetDirentry(long chan, char *name, struct DIRENTRY *pdir, long *files, long ofs, int max);
 extern char MAIN_D_801346C0;
 extern struct DIRENTRY * volatile MAIN_D_801346D0;
 int32_t sprintf(char *dst, char *fmt, ...);
@@ -3039,7 +3039,9 @@
     setMemoryCardReadError(0, slot);
   }
   else
-    if ((r == 3) || (r == 0))
+  {
+    cmd = 0xFFFF;
+    if ((r == 3) || (r == ((((((0 & cmd) & cmd) & cmd) & cmd) & cmd) & cmd)))
   {
     total = 0;
     ofs = 0;
@@ -3054,6 +3056,7 @@
   else
   {
     setMemoryCardReadError(r, slot);
+    }
   }
   return -1;
 }
