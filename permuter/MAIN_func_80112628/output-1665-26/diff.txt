--- before
+++ after
@@ -3042,14 +3042,19 @@
     if ((r == 3) || (r == 0))
   {
     total = 0;
+    if ((r && r) && r)
+    {
+    }
     ofs = 0;
-    for (i = 0; i < nfiles; i++)
-    {
-      total += ((*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + 0x18))) + 0x1FFF) / 0x2000;
+    for (i = r * 0; i < nfiles; i++)
+    {
+      i = 0x1FFF;
+      total += ((*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + 0x18))) + i) / 0x2000;
       ofs += 0x28;
     }
 
-    return total;
+    cmd = total;
+    return cmd;
   }
   else
   {
