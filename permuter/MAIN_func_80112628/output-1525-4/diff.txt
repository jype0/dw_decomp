--- before
+++ after
@@ -2852,7 +2852,7 @@
   struct DIRENTRY *next;
   char system[8];
 };
-long MemCardGetDirentry(long chan, char *name, struct DIRENTRY *pdir, long *files, long ofs, long max);
+long MemCardGetDirentry(long chan, char *name, struct DIRENTRY *pdir, long *files, long ofs, volatile int max);
 extern char MAIN_D_801346C0;
 extern struct DIRENTRY * volatile MAIN_D_801346D0;
 int32_t sprintf(char *dst, char *fmt, ...);
@@ -3030,19 +3030,20 @@
   long nfiles;
   int32_t r;
   int32_t total;
+  int new_var;
   int32_t i;
   int32_t ofs;
   MemCardSync(0, &cmd, &result);
   r = MemCardGetDirentry(channel, &(&MAIN_D_801346C0)[2], MAIN_D_801346D0, &nfiles, 0, 0xF);
+  new_var = (r == 3) || (r == 0);
   if (r == (-1))
   {
     setMemoryCardReadError(0, slot);
   }
   else
-    if ((r == 3) || (r == 0))
-  {
-    total = 0;
-    ofs = 0;
+    if (new_var)
+  {
+    total = (ofs = 0);
     for (i = 0; i < nfiles; i++)
     {
       total += ((*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + 0x18))) + 0x1FFF) / 0x2000;
