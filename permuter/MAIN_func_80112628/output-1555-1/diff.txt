--- before
+++ after
@@ -3041,6 +3041,7 @@
   else
     if ((r == 3) || (r == 0))
   {
+    setMemoryCardReadError(r, slot);
     total = 0;
     ofs = 0;
     for (i = 0; i < nfiles; i++)
@@ -3053,7 +3054,6 @@
   }
   else
   {
-    setMemoryCardReadError(r, slot);
   }
   return -1;
 }
