--- before
+++ after
@@ -3031,11 +3031,13 @@
   int32_t r;
   int32_t total;
   int32_t i;
+  char *new_var;
   int32_t ofs;
-  MemCardSync(0, &cmd, &result);
+  MemCardSync(r * 0, &cmd, &result);
   r = MemCardGetDirentry(channel, &(&MAIN_D_801346C0)[2], MAIN_D_801346D0, &nfiles, 0, 0xF);
   if (r == (-1))
   {
+    setMemoryCardReadError(r, slot);
     setMemoryCardReadError(0, slot);
   }
   else
@@ -3043,9 +3045,11 @@
   {
     total = 0;
     ofs = 0;
+    new_var = (char *) MAIN_D_801346D0;
     for (i = 0; i < nfiles; i++)
     {
-      total += ((*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + 0x18))) + 0x1FFF) / 0x2000;
+      ofs = *((int32_t *) ((new_var + ofs) + 0x18));
+      total += (ofs + 0x1FFF) / 0x2000;
       ofs += 0x28;
     }
 
@@ -3053,7 +3057,6 @@
   }
   else
   {
-    setMemoryCardReadError(r, slot);
   }
   return -1;
 }
