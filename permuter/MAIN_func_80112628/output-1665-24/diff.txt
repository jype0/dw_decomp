--- before
+++ after
@@ -3030,6 +3030,7 @@
   long nfiles;
   int32_t r;
   int32_t total;
+  int32_t new_var;
   int32_t i;
   int32_t ofs;
   MemCardSync(0, &cmd, &result);
@@ -3039,7 +3040,9 @@
     setMemoryCardReadError(0, slot);
   }
   else
-    if ((r == 3) || (r == 0))
+  {
+    new_var = r & 0xFF;
+    if ((r == 3) || (new_var == 0))
   {
     total = 0;
     ofs = 0;
@@ -3054,6 +3057,7 @@
   else
   {
     setMemoryCardReadError(r, slot);
+    }
   }
   return -1;
 }
