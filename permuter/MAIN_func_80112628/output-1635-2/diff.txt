--- before
+++ after
@@ -3030,22 +3030,26 @@
   long nfiles;
   int32_t r;
   int32_t total;
+  struct DIRENTRY * volatile *new_var;
   int32_t i;
   int32_t ofs;
   MemCardSync(0, &cmd, &result);
   r = MemCardGetDirentry(channel, &(&MAIN_D_801346C0)[2], MAIN_D_801346D0, &nfiles, 0, 0xF);
+  new_var = &MAIN_D_801346D0;
   if (r == (-1))
   {
     setMemoryCardReadError(0, slot);
   }
   else
+  {
+    result = nfiles;
     if ((r == 3) || (r == 0))
   {
     total = 0;
     ofs = 0;
-    for (i = 0; i < nfiles; i++)
-    {
-      total += ((*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + 0x18))) + 0x1FFF) / 0x2000;
+      for (i = 0; i < result; i++)
+      {
+        total += ((*((int32_t *) ((((char *) (*new_var)) + ofs) + 0x18))) + 0x1FFF) / 0x2000;
       ofs += 0x28;
     }
 
@@ -3054,6 +3058,7 @@
   else
   {
     setMemoryCardReadError(r, slot);
+    }
   }
   return -1;
 }
