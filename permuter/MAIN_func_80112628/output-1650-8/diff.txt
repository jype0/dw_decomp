--- before
+++ after
@@ -3032,18 +3032,20 @@
   int32_t total;
   int32_t i;
   int32_t ofs;
+  unsigned char new_var;
   MemCardSync(0, &cmd, &result);
   r = MemCardGetDirentry(channel, &(&MAIN_D_801346C0)[2], MAIN_D_801346D0, &nfiles, 0, 0xF);
+  new_var = 0;
   if (r == (-1))
   {
     setMemoryCardReadError(0, slot);
   }
   else
-    if ((r == 3) || (r == 0))
+    if ((r == 3) || (r == new_var))
   {
     total = 0;
-    ofs = 0;
-    for (i = 0; i < nfiles; i++)
+    ofs = new_var;
+    for (i = new_var; i < nfiles; i++)
     {
       total += ((*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + 0x18))) + 0x1FFF) / 0x2000;
       ofs += 0x28;
