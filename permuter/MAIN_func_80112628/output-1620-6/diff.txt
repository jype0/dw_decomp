--- before
+++ after
@@ -3027,6 +3027,7 @@
 {
   long cmd;
   long result;
+  int new_var;
   long nfiles;
   int32_t r;
   int32_t total;
@@ -3034,18 +3035,20 @@
   int32_t ofs;
   MemCardSync(0, &cmd, &result);
   r = MemCardGetDirentry(channel, &(&MAIN_D_801346C0)[2], MAIN_D_801346D0, &nfiles, 0, 0xF);
+  result = 0;
+  new_var = (r == 3) || (r == result);
   if (r == (-1))
   {
     setMemoryCardReadError(0, slot);
   }
   else
-    if ((r == 3) || (r == 0))
-  {
-    total = 0;
-    ofs = 0;
-    for (i = 0; i < nfiles; i++)
-    {
-      total += ((*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + 0x18))) + 0x1FFF) / 0x2000;
+    if (new_var)
+  {
+    total = result;
+    ofs = result;
+    for (i = result; i < nfiles; i++)
+    {
+      total += (0x1FFF + (*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + 0x18)))) / 0x2000;
       ofs += 0x28;
     }
 
