--- before
+++ after
@@ -3028,24 +3028,28 @@
   long cmd;
   long result;
   long nfiles;
+  int new_var;
   int32_t r;
   int32_t total;
   int32_t i;
   int32_t ofs;
   MemCardSync(0, &cmd, &result);
   r = MemCardGetDirentry(channel, &(&MAIN_D_801346C0)[2], MAIN_D_801346D0, &nfiles, 0, 0xF);
-  if (r == (-1))
+  new_var = 1;
+  if (r == (-new_var))
   {
     setMemoryCardReadError(0, slot);
   }
   else
+  {
+    result = 0x18;
     if ((r == 3) || (r == 0))
   {
     total = 0;
     ofs = 0;
     for (i = 0; i < nfiles; i++)
     {
-      total += ((*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + 0x18))) + 0x1FFF) / 0x2000;
+        total += ((*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + result))) + 0x1FFF) / 0x2000;
       ofs += 0x28;
     }
 
@@ -3054,6 +3058,7 @@
   else
   {
     setMemoryCardReadError(r, slot);
+    }
   }
   return -1;
 }
