--- before
+++ after
@@ -3036,14 +3036,17 @@
   r = MemCardGetDirentry(channel, &(&MAIN_D_801346C0)[2], MAIN_D_801346D0, &nfiles, 0, 0xF);
   if (r == (-1))
   {
-    setMemoryCardReadError(0, slot);
+    ofs = 0;
+    setMemoryCardReadError(ofs, slot);
   }
   else
-    if ((r == 3) || (r == 0))
-  {
-    total = 0;
-    ofs = 0;
-    for (i = 0; i < nfiles; i++)
+  {
+    ofs = r;
+    if ((ofs == 3) || (ofs == ofs))
+    {
+      total = ofs;
+      ofs = ofs;
+      for (i = ofs; i < nfiles; i++)
     {
       total += ((*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + 0x18))) + 0x1FFF) / 0x2000;
       ofs += 0x28;
@@ -3053,7 +3056,8 @@
   }
   else
   {
-    setMemoryCardReadError(r, slot);
+      setMemoryCardReadError(ofs, slot);
+    }
   }
   return -1;
 }
