--- before
+++ after
@@ -3028,22 +3028,23 @@
   long cmd;
   long result;
   long nfiles;
-  int32_t r;
+  unsigned char r;
   int32_t total;
   int32_t i;
   int32_t ofs;
   MemCardSync(0, &cmd, &result);
   r = MemCardGetDirentry(channel, &(&MAIN_D_801346C0)[2], MAIN_D_801346D0, &nfiles, 0, 0xF);
+  i = 0;
   if (r == (-1))
   {
-    setMemoryCardReadError(0, slot);
+    setMemoryCardReadError(i, slot);
   }
   else
-    if ((r == 3) || (r == 0))
-  {
-    total = 0;
-    ofs = 0;
-    for (i = 0; i < nfiles; i++)
+    if ((r == 3) || (r == i))
+  {
+    total = i;
+    ofs = i;
+    for (i = slot * i; i < nfiles; i++)
     {
       total += ((*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + 0x18))) + 0x1FFF) / 0x2000;
       ofs += 0x28;
