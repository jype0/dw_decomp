--- before
+++ after
@@ -3026,24 +3026,29 @@
 int32_t MAIN_func_80112628(int32_t channel, int32_t slot)
 {
   long cmd;
+  unsigned short new_var2;
   long result;
   long nfiles;
   int32_t r;
   int32_t total;
   int32_t i;
+  int new_var;
+  long *new_var3;
   int32_t ofs;
-  MemCardSync(0, &cmd, &result);
+  new_var3 = &result;
+  MemCardSync(0, &cmd, new_var3);
   r = MemCardGetDirentry(channel, &(&MAIN_D_801346C0)[2], MAIN_D_801346D0, &nfiles, 0, 0xF);
+  new_var2 = (r == 3) || (r == 0);
   if (r == (-1))
   {
     setMemoryCardReadError(0, slot);
   }
   else
-    if ((r == 3) || (r == 0))
+    if (new_var2)
   {
     total = 0;
     ofs = 0;
-    for (i = 0; i < nfiles; i++)
+    for (i = (new_var = 0); (i < nfiles) & 0xFFFFu; i++)
     {
       total += ((*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + 0x18))) + 0x1FFF) / 0x2000;
       ofs += 0x28;
@@ -3054,6 +3059,9 @@
   else
   {
     setMemoryCardReadError(r, slot);
+  }
+  if (slot && slot)
+  {
   }
   return -1;
 }
