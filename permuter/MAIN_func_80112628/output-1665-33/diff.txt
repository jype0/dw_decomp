--- before
+++ after
@@ -3032,6 +3032,7 @@
   int32_t total;
   int32_t i;
   int32_t ofs;
+  int32_t new_var;
   MemCardSync(0, &cmd, &result);
   r = MemCardGetDirentry(channel, &(&MAIN_D_801346C0)[2], MAIN_D_801346D0, &nfiles, 0, 0xF);
   if (r == (-1))
@@ -3039,11 +3040,13 @@
     setMemoryCardReadError(0, slot);
   }
   else
-    if ((r == 3) || (r == 0))
   {
     total = 0;
+    if ((r == 3) || ((new_var = r) == total))
+    {
+      total = total;
     ofs = 0;
-    for (i = 0; i < nfiles; i++)
+      for (i = total; i < nfiles; i++)
     {
       total += ((*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + 0x18))) + 0x1FFF) / 0x2000;
       ofs += 0x28;
@@ -3054,6 +3057,7 @@
   else
   {
     setMemoryCardReadError(r, slot);
+    }
   }
   return -1;
 }
