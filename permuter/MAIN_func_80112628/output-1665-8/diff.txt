--- before
+++ after
@@ -3039,11 +3039,13 @@
     setMemoryCardReadError(0, slot);
   }
   else
+  {
+    cmd = 0;
     if ((r == 3) || (r == 0))
   {
     total = 0;
     ofs = 0;
-    for (i = 0; i < nfiles; i++)
+      for (i = cmd; i < nfiles; i++)
     {
       total += ((*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + 0x18))) + 0x1FFF) / 0x2000;
       ofs += 0x28;
@@ -3054,6 +3056,7 @@
   else
   {
     setMemoryCardReadError(r, slot);
+    }
   }
   return -1;
 }
