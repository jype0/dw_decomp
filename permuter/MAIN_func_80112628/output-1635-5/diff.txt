--- before
+++ after
@@ -3027,6 +3027,7 @@
 {
   long cmd;
   long result;
+  int new_var;
   long nfiles;
   int32_t r;
   int32_t total;
@@ -3034,14 +3035,16 @@
   int32_t ofs;
   MemCardSync(0, &cmd, &result);
   r = MemCardGetDirentry(channel, &(&MAIN_D_801346C0)[2], MAIN_D_801346D0, &nfiles, 0, 0xF);
+  new_var = (r == 3) || (r == 0);
   if (r == (-1))
   {
-    setMemoryCardReadError(0, slot);
+    result = 0;
+    setMemoryCardReadError(result, slot);
   }
   else
-    if ((r == 3) || (r == 0))
-  {
-    total = 0;
+    if (new_var)
+  {
+    total = result;
     ofs = 0;
     for (i = 0; i < nfiles; i++)
     {
