--- before
+++ after
@@ -3031,19 +3031,24 @@
   int32_t r;
   int32_t total;
   int32_t i;
+  char *new_var;
   int32_t ofs;
+  new_var = &(&MAIN_D_801346C0)[2];
   MemCardSync(0, &cmd, &result);
-  r = MemCardGetDirentry(channel, &(&MAIN_D_801346C0)[2], MAIN_D_801346D0, &nfiles, 0, 0xF);
-  if (r == (-1))
+  r = MemCardGetDirentry(channel, new_var, MAIN_D_801346D0, &nfiles, 0, 0xF);
+  i = r == (-1);
+  if (i)
   {
     setMemoryCardReadError(0, slot);
   }
   else
+  {
+    cmd = nfiles;
     if ((r == 3) || (r == 0))
   {
     total = 0;
     ofs = 0;
-    for (i = 0; i < nfiles; i++)
+      for (i = 0; i < cmd; i++)
     {
       total += ((*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + 0x18))) + 0x1FFF) / 0x2000;
       ofs += 0x28;
@@ -3055,7 +3060,8 @@
   {
     setMemoryCardReadError(r, slot);
   }
-  return -1;
+  }
+  return (short) (-1);
 }
 
 int32_t MAIN_func_8011296C(MenuCursor *cursor, int32_t which);
