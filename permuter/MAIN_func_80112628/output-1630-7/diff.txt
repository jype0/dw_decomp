--- before
+++ after
@@ -3027,25 +3027,29 @@
 {
   long cmd;
   long result;
+  char *new_var;
   long nfiles;
-  int32_t r;
+  int new_var2;
+  unsigned short r;
   int32_t total;
   int32_t i;
   int32_t ofs;
   MemCardSync(0, &cmd, &result);
-  r = MemCardGetDirentry(channel, &(&MAIN_D_801346C0)[2], MAIN_D_801346D0, &nfiles, 0, 0xF);
+  new_var2 = 0;
+  r = MemCardGetDirentry(channel, &(&MAIN_D_801346C0)[2], MAIN_D_801346D0, &nfiles, new_var2, 0xF);
   if (r == (-1))
   {
-    setMemoryCardReadError(0, slot);
+    setMemoryCardReadError(new_var2, slot);
   }
   else
-    if ((r == 3) || (r == 0))
-  {
-    total = 0;
-    ofs = 0;
-    for (i = 0; i < nfiles; i++)
-    {
-      total += ((*((int32_t *) ((((char *) MAIN_D_801346D0) + ofs) + 0x18))) + 0x1FFF) / 0x2000;
+    if ((r == 3) || (r == new_var2))
+  {
+    total = new_var2;
+    ofs = new_var2;
+    for (i = new_var2; i < nfiles; i++)
+    {
+      new_var = (char *) MAIN_D_801346D0;
+      total += ((*((int32_t *) ((new_var + ofs) + 0x18))) + 0x1FFF) / 0x2000;
       ofs += 0x28;
     }
 
@@ -3055,6 +3059,7 @@
   {
     setMemoryCardReadError(r, slot);
   }
+ if (1) { }
   return -1;
 }
 
